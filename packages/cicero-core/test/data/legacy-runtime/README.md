# Legacy Runtime

A template as it was written against cicero 1.x: `accordproject.cicero` is
`^1.0.0` and the model imports `org.accordproject.runtime@0.2.0`. It is not
loadable by this version of cicero-core, which is the point of the fixture.
